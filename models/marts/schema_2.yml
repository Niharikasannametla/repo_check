
version: 2

models:
  - name: dim_customers
    config:
      contract:
        enforced: true
    columns:
      - name: id1
        data_type: integer
      - name: id2
        data_type: integer
